name: "Run flake8"

on:
  push:
    branches: [ master, main, workflow ]
  pull_request:
    branches: [ master, main, workflow ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Cache modules
      uses: actions/cache@v2
      env:
        cache-name: cache-python-modules
      with:
        path: ~/.cache/pip
        VERSION=$(flake8 --version)
        key: ${{ runner.os }}-build-${{ env.cache-name }}-flake8-$VERSION

    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8
        
    - name: Run flake8 on the project
      run: |
        flake8 .

    # need multiple caches, one for flake8, one for isort



    
